{
  "name": "g5y-noauth",
  "dependencies": {
    "g5y-gateway-api": "*",
    "g5y-sidecar": "file:../../sidecar"
  },
  "devDependencies": {
    "vitest": "3.1.4"
  },
  "scripts": {
    "setup": "KUBECONFIG=../../teststate/kubeconfig kubectl get pods && false",
    "test": "vitest run"
  }
}
