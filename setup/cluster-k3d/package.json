{
  "name": "g5y-cluster-k3d",
  "dependencies": {
    "g5y-kubie-not": "*"
  },
  "scripts": {
    "clean:buildx": "docker buildx rm k3s-buildkit 2>/dev/null || true",
    "clean": "npm run clean:buildx && k3d cluster delete test-g5y && rm ../../teststate/kubeconfig-k3d || true",
    "setup": "./bin/g5y-k3d.sh"
  }
}
