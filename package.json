{
  "private": true,
  "name": "g5y-dev",
  "workspaces": [
    "internal",
    "filterapi",
    "cmd",
    "sidecar",
    "setup/*",
    "checks/*",
    "examples/*"
  ],
  "packageManager": "npm@10.9.2",
  "bin": {
    "foo": "echo FOO"
  },
  "scripts": {
    "clean": "docker rmi $(docker images --format 'table {{.Repository}}:{{.Tag}}' | grep 'g5y' | tr -s ' ')",
    "start:dev": "skaffold run --status-check=false --tail=true"
  },
  "devDependencies": {
  }
}
