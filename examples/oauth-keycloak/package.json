{
  "name": "g5y-oauth-keycloak",
  "dependencies": {
    "g5y-noauth": "file:../noauth",
    "g5y-image-builder": "file:../../setup/image-builder"
  },
  "devDependencies": {
    "vitest": "3.1.4"
  },
  "scripts": {
    "clean": "rm -rf target",
    "build:images": "./build-image.sh",
    "build:dev": "buildctl-metadata-to-kustomize src target/dev-kustomize ../../sidecar/target/buildkit-image.json ./target/buildkit-image.json",
    "build": "npm run build:images && npm run build:dev",
    "setup": "KUBECONFIG=../../teststate/kubeconfig apply -k target/dev-kustomize",
    "test": "vitest run",
    "zzz": "printenv"
  }
}
